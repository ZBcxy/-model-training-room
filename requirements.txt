# ============================================================
# Model Training Room (模型训练室) - Dependencies
# ============================================================

# --- Web Framework ---
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
gradio>=4.29.0
python-multipart>=0.0.9

# --- Model Hub ---
huggingface_hub>=0.23.0
modelscope>=1.14.0

# --- Training Engine (LLaMA-Factory) ---
# LLaMA-Factory will be installed from source, see setup instructions
# Dependencies it needs:
torch>=2.1.0
transformers>=4.41.0
datasets>=2.18.0
accelerate>=0.28.0
peft>=0.10.0
trl>=0.8.0

# --- Dataset Processing ---
pandas>=2.2.0
pyarrow>=15.0.0
scikit-learn>=1.4.0

# --- GGUF Export ---
# llama-cpp-python provides GGUF conversion and quantization
llama-cpp-python>=0.2.75

# --- GPU Monitoring ---
nvidia-ml-py>=12.535.0
psutil>=5.9.0

# --- Database ---
# SQLite is built-in (stdlib sqlite3)

# --- Utilities ---
tqdm>=4.66.0
rich>=13.7.0
pydantic>=2.7.0
httpx>=0.27.0
aiofiles>=23.2.0
